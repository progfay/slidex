# slidex — スライド生成規約

Claude Code は **slides/ 以下の生成・編集**が主な仕事で、engine/ と design-system/ は明示的な指示がない限り変更しない。
対応ブラウザは最新の Chromium 系のみ(詳細は [README.md](./README.md#動作要件))。

変更は意味のあるまとまりごとに git commit する(未コミットのまま溜めない)。

## スライドを作る流れ

デッキは次の5ステップで作る。

ステップは一方通行ではない。前のステップに戻って直してよい。
**正は常に一番下流のドキュメント**であり、上流ファイルへの逆同期は任意。

### 1. ブレインダンプを書く — `braindump.md` / `research.md`

ユーザーに書かせるのではなく、まず AI が対話する。話したい理由・想定聴衆・
伝えたいエピソード・持ち帰ってほしいことを掘り下げ、**コンセプトの輪郭が
見えてくるまで十分に議論する**(聞き役に徹し、早々にまとめない)。

**この段階では発表・伝え方を一切意識しない。**「どう話したいか」
「どんな構成で伝えるか」を聞くと、伝えたい内容そのものより先に発表を
意識した雑念が入り込み、素材が濁る。braindump で聞くのは中身
(理由・エピソード・話したいこと)だけで、それ以外は以降のステップで考える。

出てきた要点・語り・素材を `braindump.md` に書き出す(リポジトリルート、
構成は気にせず思いつくまま)。

対話の中でユーザーの主張・エピソード・数値に裏付けが要りそうなものが出てきたら、
その場でリサーチして真偽・出典を確認する。裏付けが取れた事実や引用は出典つきで
`research.md` に書き出す。矛盾する情報が見つかった場合は断定せずユーザーに伝え、
どちらを採るか確認する。有料記事・社内資料・個人の体験など
Claude 側で調査できない主張は、ユーザーにソースの提示を求めてよい
(裏が取れない主張は無理に research.md へ書かず、braindump.md 側に留める)。

### 2. コンセプトをまとめる — `concept.md`

`braindump.md` から核を抽出し `concept.md` に書く:

- 一言メッセージ(聞き手に持ち帰ってほしいことを1文で)
- 想定聴衆
- 目的(伝える・説得する・行動を促す、など)
- 根拠・柱(2〜3個)

braindump.md を材料に、どうまとめるかはユーザーと対話しながら決める。

ここがぶれるとスライド構成全体がぶれるので、確定したら
ユーザーに確認を取り、`manifest.json` の `title` をデッキ名に書き換える。

### 3. HTML スライドに書き起こす

`concept.md` を軸に、スライド単位の構成を `slides/NN-slug.html` へ直接書き起こす
(1スライド = 1ファイル)。`manifest.json` の `slides` 配列を並び順どおり更新する。

まずは箇条書きのみで話す内容の流れを組み立てる段階であり、発表者ノートやリッチ化
(装飾・図解)は次のステップ以降で行う。スライドごとに書くのは:

- そのスライドの一言メッセージ
- 画面に載せる要点(見出し + 箇条書き)。**braindump の文章をそのまま流し込まない**、
  要点だけに絞る(「見出し + 箇条書き4項目」を上限目安にする)

`braindump.md` の素材の取捨選択・並べ替えはここで行う。判断が割れやすいので、
外した素材や構成の順序案は理由を添えてユーザーと相談する。確定したらユーザーに確認を取る(ここから先は直すコストが上がる)。

**デッキの最後は必ず「まとめ」スライドにする**(`concept.md` の一言メッセージ・
根拠/柱をベースに箇条書きでまとめる)。

共通レイアウトクラスは存在しないので、スライドごとに `<style>` で
適切な構図(中央寄せ・全面画像・引用など)を組む。

### 4. ノートを書く

各スライドの `<aside class="notes">` に、発表中にしゃべる内容を発表者ノートとして書く。
**このステップで書き換えるのはノートだけ**で、画面に表示される見出しや箇条書きには
手を入れない。

発表者ノートは口語体で書く(基本はですます調)。実際に話す言葉に近い形で書くことで、
読み上げたときに自然になる。

`braindump.md` の語りを土台に、`research.md` の事実・出典を根拠として組み込みながら、
スライドからスライドへのつながりも含めて話の流れを言語化する。ノートを書くことで
話の流れが言語化され、スライドに何を書き残すべきかがより研ぎ澄まされる。書きながら
「これはスライド側にも出すべきだ」「この箇条書きは要らない」と気づいたら、ステップ3
に戻ってスライドの見出し・箇条書きを調整してよい。

### 5. リッチ化する

素の書き起こしを、HTML の柔軟性を活かして分かりやすく・見やすくする。

- 箇条書きの図解化(スライド固有の `<style>` での図・対比・タイムライン)
- コードハイライト、`color: var(--color-accent)` での強調、CSS による演出
- ただし規約は維持する: 生の値ではなくデザイントークンを使い、
  1スライドの情報量は増やさない。**リッチ化 = 装飾の追加ではなく伝達効率の改善**

## スライドHTML

HTML構造の契約・デザイントークンは skill `slide-html` を参照
(ステップ3・5、既存スライド修正時に読む)。
